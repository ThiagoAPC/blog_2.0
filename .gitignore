# Astro build output and cache
site/.astro/
site/dist/
site/node_modules/

# package-lock.json is platform-specific (Alpine musl vs Ubuntu gnu for Rollup)
# CI generates its own fresh lockfile — do NOT commit this
site/package-lock.json

# Environment files — NEVER commit secrets
.env
.env.local
.env.*.local
**/.env
**/.env.local

# OS
.DS_Store
Thumbs.db
desktop.ini

# npm logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# IDE
.vscode/settings.json
.idea/
*.swp
*.swo

# Docker
.dockerenv
